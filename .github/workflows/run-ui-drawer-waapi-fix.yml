name: Apply robust drawer animation

on:
  push:
    branches:
      - feat/ui-subscription-dashboard
    paths:
      - scripts/fix-ui-drawer-waapi.js
      - .github/workflows/run-ui-drawer-waapi-fix.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/ui-subscription-dashboard
          fetch-depth: 0
      - name: Apply drawer animation fix
        run: node scripts/fix-ui-drawer-waapi.js
      - name: Validate JavaScript
        run: node --check public/js/redesign.js
      - name: Remove temporary helpers
        run: rm -f scripts/fix-ui-drawer-waapi.js .github/workflows/run-ui-drawer-waapi-fix.yml
      - name: Commit drawer animation fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(ui): animate drawer with browser animation API"
          git push origin HEAD:feat/ui-subscription-dashboard
